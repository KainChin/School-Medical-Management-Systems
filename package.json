{
  "name": "School-Medical-Management-System",
  "version": "1.0.0",
  "dependencies": {
    "@react-oauth/google": "^0.12.2",
    "@tailwindcss/vite": "^4.1.11",
    "jwt-decode": "^4.0.0",
    "tailwindcss": "^4.1.11",
    "vite": "^4.1.0",  // Thêm Vite để chạy build và dev server
    "react": "^18.0.0",  // Đảm bảo React được cài đặt nếu chưa có
    "react-dom": "^18.0.0"  // Đảm bảo ReactDOM được cài đặt nếu chưa có
  },
  "scripts": {
    "dev": "vite",  // Lệnh chạy trong môi trường phát triển
    "build": "vite build",  // Lệnh build cho môi trường sản xuất
    "preview": "vite preview"  // Lệnh preview ứng dụng sau khi build
  },
  "devDependencies": {
    "vite": "^4.1.0",  // Vite là công cụ build mà bạn sẽ sử dụng
    "tailwindcss": "^4.1.11",  // Tailwind CSS cho việc styling
    "@tailwindcss/vite": "^4.1.11"  // Plugin cho Vite
  }
}
